#include <iostream>
using namespace std;

int main(){
    int heights[231];
    int i, j, k, times, tests, height;

    cin >> tests;
    for (i=0; i<= tests; i++)
    {
        for(k=0;k<=230;k++) heights[k];
        cin >> times;
        for (j=0;j<=times;j++) heights[j]++;
    }

    return 0;
}
